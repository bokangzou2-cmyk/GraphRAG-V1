# GraphRAG-V1 Project Retrospective

审计日期：2026-06-01  
定位：面向面试官和技术评审的项目实施复盘。

## 项目定位

GraphRAG-V1 是一个面向刑法样本数据的 GraphRAG 原型。项目目标不是覆盖全量法律数据库，而是在约 150 个案例和刑法条文样本范围内，验证一套完整的法律检索增强问答链路：

1. 可追溯的数据解析。
2. 法条和案例的结构化增强。
3. 面向问答的 chunk 与 embedding。
4. 案例、罪名、法条之间的图谱关系。
5. lexical/vector/graph 混合检索。
6. Query Understanding 和多轮问题改写。
7. Provider 生成、引用约束和 fallback。
8. 前端问答、历史记录和回答依据展示。
9. 自动评测、对抗评测和隐藏验收。

## 1. 数据解析

为什么做：  
法律问答不能只依赖原始文本，需要先把刑法条文和裁判文书拆成可追溯的数据记录。

如何实现：  
通过 `processing/parse_law_sample.py` 和 `processing/parse_cases_sample.py` 解析 raw 数据，生成结构化 JSONL。raw 数据保持不可变，生成数据放在 `processing_data/`。

当前效果：  
样本级数据已经能支持后续 enrich、chunk、graph 和 retrieval。

优势：  
数据层和原始文件分离，便于重建和审计。

局限：  
解析逻辑仍服务于样本数据，对不同来源、不同格式、不同法院文书风格的泛化能力未验证。

## 2. Enrich

为什么做：  
原始裁判文书中事实、证据、辩护、法院认定、裁判理由和适用法条混在长文本里，直接检索会造成召回粗糙。

如何实现：  
`processing/enrich_cases_sample.py` 抽取案件字段、罪名、引用法条、裁判结果等信息，并保留抽取方法和来源。

当前效果：  
已经能按字段构建 case chunks，并支持意图匹配，例如查事实、查证据、查裁判理由。

优势：  
字段级结构让检索更贴近法律问答场景。

局限：  
规则抽取仍较多，复杂文书和多罪名、多被告、多金额角色场景下会出现边界问题。

## 3. Chunk

为什么做：  
RAG 需要合适粒度的文本单元。法律文本既不能切得太碎，也不能把事实、证据、理由混成一块。

如何实现：  
使用 `chunk_cases_sample.py` 和 `chunk_law_sample.py` 生成案例字段 chunk 与法条 chunk，保留 `field`、`case_id`、`article_no`、`source_file`、`text_hash` 等字段。

当前效果：  
chunk 已经能支撑字段级检索和 citation 展示。

优势：  
字段来源清楚，便于解释“为什么召回这段”。

局限：  
长句切分、枚举结构、裁判理由内部层次仍有提升空间。样本级已可用，全量时需要更强的文书结构解析。

## 4. Graph

为什么做：  
法律问答经常需要从罪名找到案例，从案例找到适用法条，从法条反查相关案例。纯文本检索不够稳定。

如何实现：  
`build_graph_sample.py` 构建 graph nodes/edges，`graph_retriever_sample.py` 基于图谱做候选召回，保留 edge 的 raw citation、normalized citation、law version 等信息。

当前效果：  
图谱已经参与 hybrid candidate generation，并能输出 graph paths。

优势：  
图谱提供了跨实体召回能力，尤其适合法条到案例、案例到法条、多跳解释。

局限：  
图谱 schema 和运行时访问层仍在 sample 脚本中。历史法条、司法解释、未知引用在更大数据下仍需要更严格的规范化验证。

## 5. Embedding

为什么做：  
用户问题常常是生活化表达，例如“入室盗窃”“过失让人死亡”，不能完全依赖关键词。

如何实现：  
`embed_retrieval_chunks_sample.py` 为 retrieval chunks 构建向量索引，`vector_search_sample.py` 提供向量召回。

当前效果：  
向量召回已经作为 hybrid 的一路候选来源。

优势：  
提高同义表达、模糊描述和字段语义检索能力。

局限：  
embedding 模型、chunk 粒度、中文法律语义之间仍可能不匹配。全量时还要考虑索引规模、更新策略和向量漂移。

## 6. Retrieval

为什么做：  
法律问答需要高精度引用。单一检索方式容易漏召回或引入泛化法条。

如何实现：  
`hybrid_search_sample.py` 合并：

- lexical retrieval
- vector retrieval
- graph retrieval
- case title exact match
- article exact match
- crime exact match
- intent field match
- law version constraints

当前效果：  
样本范围内已经能处理法条、案例事实、证据、理由、量刑、金额相似案例等问题。

优势：  
召回来源可解释，排序理由可追踪。

局限：  
权重仍偏工程调参。全量场景需要学习排序或更系统的 evaluation-driven ranking。

## 7. Hybrid

为什么做：  
GraphRAG 的关键不是“有图”，而是让图召回、文本召回、向量召回协同工作。

如何实现：  
hybrid 层把不同候选合并，保留来源、score、candidate_type、graph_paths 和 diagnostics。

当前效果：  
图召回已经从后处理解释升级为候选生成器。

优势：  
能够回答 case -> article、article -> cases、crime -> cases 等多跳问题。

局限：  
目前没有统一的强类型 RetrievalResult 合同文件，字段主要靠约定。

## 8. Answer

为什么做：  
检索结果不能直接丢给用户，需要根据引用生成简洁、可靠、带依据的回答。

如何实现：  
当前有三条回答相关路径：

- `answer_sample.py`：早期 retrieval-only baseline。
- `app/llm/chat_service.py`：stable chat。
- `langchain_orchestration_sample.py`：当前前端主链路。

当前效果：  
能生成基于资料的回答，也能在 Provider 不可用时 fallback 到检索摘要。

优势：  
回答和 citation 绑定，降低无依据生成风险。

局限：  
回答链路重复，是当前最明显的架构债之一。

## 9. Router

为什么做：  
不是所有问题都应该走同一套检索。例如刑法常识、法条查询、案例追问、违法规避、非刑法问题应有不同处理。

如何实现：  
早期使用 router LoRA 和规则判断是否检索、检索范围。后续演进为 Query Understanding，输出问题改写、实体识别、检索意图、上下文需求和 answer policy。

当前效果：  
已经能改善多轮问题和生活化表述，但仍依赖规则兜底。

优势：  
方向正确，能把“用户怎么说”转成“系统怎么检索”。

局限：  
训练数据覆盖决定上限。直接法律常识、口语罪名、上下文引用都需要更多高质量样本。

## 10. LLM

为什么做：  
Provider 用于把检索资料组织成自然语言回答，同时遵守 citation 和 refusal 约束。

如何实现：  
通过历史命名的 `app/llm/minimax_client.py` 调用 DeepSeek/OpenAI-compatible Provider，通过 `app/llm/prompting.py` 管理 JSON contract 和 grounding 要求。当前默认配置为 `LLM_PROVIDER=deepseek`、`LLM_MODEL=deepseek-v4-flash`。

当前效果：  
Provider 可用时回答体验更自然；不可用时系统可以 fallback。

优势：  
没有把 Provider 当作唯一真相，仍保留 retrieval-only baseline。

局限：  
Provider 网络、格式、误拒答、引用减少都是高风险。面向用户时不应暴露技术错误。

## 11. API

为什么做：  
需要把脚本能力封装为可交互产品，而不是只在命令行验证。

如何实现：  
FastAPI 提供 health、auth、retrieval、chat、orchestration、conversations 等接口。

当前效果：  
前端可以登录、提问、查看依据、保存历史、删除会话、撤销上条消息。

优势：  
已经有完整产品闭环。

局限：  
stable chat 和 orchestration chat 的边界不够清楚。运行时仍直接 import processing 模块。

## 12. Frontend

为什么做：  
GraphRAG 项目如果只有脚本，不利于展示用户体验和回答依据。

如何实现：  
Vite + React 前端提供登录、会话列表、问答输入、回答展示和依据面板。

当前效果：  
可以作为演示界面使用，用户能看到引用资料和提示。

优势：  
降低项目展示门槛，也便于发现真实用户问题。

局限：  
`App.tsx` 职责偏重，回答依据仍需进一步产品化表达，但本轮不做 UI 优化。

## 13. Eval

为什么做：  
GraphRAG 很容易“看起来能答”，但实际检索错误、引用错误或拒答错误。必须有评测。

如何实现：  
保留 lexical、vector、hybrid、graph、answer、orchestration、backend tests 等脚本。

当前效果：  
能在修改后做回归，防止核心链路大面积退化。

优势：  
评测体系覆盖了多个层级，不只看最终回答。

局限：  
评测集仍来自样本范围，存在对样本分布过拟合的风险。

## 14. Adversarial Eval

为什么做：  
法律系统必须识别违法规避、编造案例、非刑法、无答案等问题。

如何实现：  
通过 adversarial eval 和 provider regression 检查拒答、answer_type、citation、fallback。

当前效果：  
能暴露拒答和 Provider 风险。

优势：  
比普通 QA 更接近真实失败场景。

局限：  
对抗问题的设计需要持续扩充，否则容易被已有规则覆盖。

## 15. Hidden Eval

为什么做：  
多轮调参后，公开 eval 可能被系统过拟合。Hidden eval 用于最终验收。

如何实现：  
生成独立 hidden eval sample 和报告，不复用公开 answer/adversarial 题目。

当前效果：  
作为 anti-overfitting 验收资产存在。

优势：  
能更真实地衡量系统泛化质量。

局限：  
如果 hidden eval 也被长期用于调参，它也会失去隐藏测试价值。

## 已完成

- 样本数据处理闭环。
- law/case chunk 与 retrieval chunks。
- graph nodes/edges 构建。
- lexical/vector/graph/hybrid 检索。
- Query Understanding 初步统一。
- Provider JSON contract 与 fallback。
- 前端登录、问答、历史和依据展示。
- 多层级 eval 与 adversarial/hidden 验收资产。

## 已验证

- 样本级 pipeline 可以生成检索和图谱所需产物。
- API 可以支撑网页交互。
- 混合检索比单一路径更适合刑法问答。
- 图谱对法条、案例、多跳查询有明显价值。
- 用户手测能快速暴露多轮上下文、金额语义、Provider 失败等真实问题。

## 需要优化

- 把运行时模块从 `processing/*_sample.py` 中收敛到正式 service 层。
- 统一 stable chat 和 orchestration chat 的 answer contract。
- 增强 Query Understanding 训练数据，尤其是多轮改写、生活化罪名、直接刑法常识。
- 结构化金额角色和案件相似度。
- Provider 错误对用户完全静默，内部记录给审计。

## 不建议继续投入

- 不建议继续为每个手测问题单独加零散规则。
- 不建议在 sample 命名和双链路未收敛前盲目扩大全量。
- 不建议把 Provider 作为唯一答案来源。
- 不建议继续扩大前端功能，直到后端 answer contract 稳定。

## 下一阶段最值得做的三件事

1. 运行时架构收敛  
   新建正式 retrieval/query/answer service 包装层，先包装现有逻辑，不重写算法。

2. Query Understanding 二期  
   用 GPT 生成和人工审查结合的训练集，重点覆盖多轮改写、案件引用、金额追问、生活化罪名、直接法律常识。

3. 扩样本前的数据质量门禁  
   在 1000 案例前先固定 schema、manifest、quality report、law normalization、amount role 和 eval 基准。

## 到不同规模还缺什么

### 到 1000 案例

- 更稳的 parser/enrich 失败记录。
- 更强的金额角色抽取。
- 更清晰的 retrieval result schema。
- eval 集按罪名、字段、金额、法条版本分层。

### 到 10000 案例

- 增量 pipeline。
- 向量索引构建和更新策略。
- 图谱边去重、版本化、质量统计。
- 检索性能监控。
- 样本偏差分析。

### 到全量案例

- 数据治理和数据来源合规。
- 分布式索引或专业向量库。
- 图数据库或可扩展 graph store。
- 自动化质量巡检。
- Provider 成本、限流、降级策略。
- 法律风险声明和产品边界。

## 最终复盘结论

GraphRAG-V1 的核心价值在于它不是单点 demo，而是覆盖了数据、图谱、检索、回答、评测、前端的完整样本级工程闭环。当前最应该展示的是“如何把非结构化法律文本变成可追溯、可评测、可交互的 GraphRAG 系统”。

它的主要短板也很清楚：代码结构仍保留 sample 阶段痕迹，运行时和离线处理耦合，回答链路重复，多轮上下文和 Provider 稳定性仍是最容易失效的地方。

作为简历项目，它已经具备技术深度和工程完整性；作为生产系统，它还需要架构收敛、数据规模验证和法律风险控制。
