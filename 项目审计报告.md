# GraphRAG-V1 Project Audit Report

审计日期：2026-06-01  
审计范围：`app/`、`processing/`、`frontend/`、`tests/`、`docs/`、根目录配置与项目文档。  
审计原则：本轮只做静态审计和复盘，不新增功能、不重构、不调整检索/回答逻辑、不运行长期评测。

## 结论摘要

当前项目已经从早期“小样本闭环”演进成了一个可演示、可评测、可继续扩展的样本级刑法 GraphRAG 原型。核心能力包括样本数据处理、法条/案例 chunk、图谱边构建、词法/向量/图混合检索、问答编排、Provider 兜底、前端交互和审计日志。

主要问题不是能力缺失，而是演进过程中形成了明显的历史层叠：

- 运行时后端直接依赖 `processing/` 下的 sample 脚本，模块边界不够正式。
- `answer_sample`、`chat_service`、`langchain_orchestration_sample` 三条回答路径职责重叠。
- Router/Query Understanding 同时存在旧 router LoRA、新 Query Understanding LoRA、规则后处理、scope guard，边界需要收敛。
- `*_sample.py` 命名仍大量存在，但其中部分已经是正式演示链路的核心模块。
- 评测、训练、修复脚本较多，保留了项目迭代痕迹，但对新维护者不够清晰。

本项目可以作为简历和技术评审项目展示，但应明确表述为“高质量样本级 GraphRAG 原型”，而不是生产级全量系统。

## 文件级审计

说明：

- 使用状态：`运行时` 表示被 API 或前端实际调用；`流水线` 表示参与样本数据生成；`评测` 表示用于回归和质量验证；`训练` 表示用于 LoRA/数据集；`文档/配置` 表示非代码运行路径。
- 删除建议：`保留`、`暂不删除`、`可归档`、`可删除`。
- 删除风险：`高` 表示会破坏当前演示或评测；`中` 表示会影响回归、训练或排障；`低` 表示主要影响文档或历史记录。

### 根目录

| 文件 | 作用 | 使用状态 | 核心路径 | 重复/废弃/技术债 | 删除建议 | 删除风险 |
|---|---|---:|---:|---|---|---:|
| `AGENTS.md` | Codex 项目协作规则 | 文档/配置 | 否 | 包含项目数据处理约束，仍有参考价值 | 保留 | 中 |
| `.env.example` | 环境变量示例 | 配置 | 是 | 需要与实际 Provider/Auth 配置保持同步 | 保留 | 中 |
| `.gitignore` | Git 忽略规则 | 配置 | 是 | 无明显问题 | 保留 | 高 |
| `pyproject.toml` | Python 依赖和测试配置 | 配置 | 是 | 依赖边界需与前后端启动文档同步 | 保留 | 高 |
| `PROJECT_PLAN.md` | 历史项目计划 | 文档 | 否 | 可能与当前真实架构不完全一致 | 可归档 | 低 |
| `FINAL_BASELINE_REPORT.md` | 最终基线报告 | 文档/审计 | 否 | 是冻结基线，应保留 | 保留 | 中 |
| `PROJECT_AUDIT_REPORT.md` | 本审计报告 | 文档 | 否 | 当前输出 | 保留 | 中 |

### `app/`

| 文件 | 作用 | 使用状态 | 核心路径 | 重复/废弃/技术债 | 删除建议 | 删除风险 |
|---|---|---:|---:|---|---|---:|
| `app/main.py` | FastAPI 应用入口，挂载 health/auth/retrieval/chat/conversations | 运行时 | 是 | CORS 与路由集中在入口，结构清楚 | 保留 | 高 |
| `app/config.py` | 环境变量、路径和 Provider 配置 | 运行时 | 是 | 配置项集中，但 Provider 命名与实际 DeepSeek/OpenAI-compatible 关系需文档化 | 保留 | 高 |
| `app/auth.py` | 登录认证、token、用户校验 | 运行时 | 是 | 当前更偏演示级，用户体系简单 | 保留 | 高 |
| `app/chat_history.py` | SQLite 会话、消息、回答依据持久化 | 运行时 | 是 | 当前是产品体验核心，但需要数据保留策略 | 保留 | 高 |
| `app/audit_log.py` | 问答审计运行记录 | 运行时 | 是 | 与 chat history 有部分重叠，但用途不同 | 保留 | 中 |
| `app/schemas.py` | API 请求/响应模型 | 运行时 | 是 | 需要持续作为前后端合同源 | 保留 | 高 |
| `app/processing_bridge.py` | 把 `processing/` 注入 Python path | 运行时 | 是 | 关键技术债：运行时后端直接依赖 sample 脚本目录 | 暂不删除 | 高 |
| `app/api/routes/health.py` | `/api/health` 状态检查 | 运行时 | 是 | 健康检查更多检查文件存在，不代表质量通过 | 保留 | 高 |
| `app/api/routes/auth.py` | `/api/auth/login` 等认证接口 | 运行时 | 是 | 与 `auth.py` 配合，职责清楚 | 保留 | 高 |
| `app/api/routes/chat.py` | `/api/chat` 和 `/api/chat/orchestration` | 运行时 | 是 | stable 与 orchestration 两条链路并存，存在回答逻辑漂移风险 | 保留，后续收敛 | 高 |
| `app/api/routes/retrieval.py` | `/api/retrieval/search` 混合检索接口 | 运行时 | 是 | 直接调用 `hybrid_search_sample`，命名不正式 | 保留，后续迁移 | 高 |
| `app/api/routes/conversations.py` | 会话列表、删除、撤销等接口 | 运行时 | 是 | 产品体验核心，依赖 chat history | 保留 | 高 |
| `app/llm/chat_service.py` | stable chat 的回答服务 | 运行时 | 是 | 与 orchestration 和 `answer_sample` 有职责重叠 | 保留，后续收敛 | 高 |
| `app/llm/minimax_client.py` | OpenAI-compatible Provider 客户端 | 运行时 | 是 | Provider 错误与重试策略是高风险点 | 保留 | 高 |
| `app/llm/prompting.py` | Provider prompt 和 JSON 合同 | 运行时 | 是 | Prompt 与 orchestration 中的逻辑仍有分散 | 保留，后续集中 | 高 |

### `processing/` 数据流水线

| 文件 | 作用 | 使用状态 | 核心路径 | 重复/废弃/技术债 | 删除建议 | 删除风险 |
|---|---|---:|---:|---|---|---:|
| `common.py` | JSONL、hash、路径等通用工具 | 流水线/运行时 | 是 | 多脚本依赖 | 保留 | 高 |
| `parse_law_sample.py` | 解析刑法样本条文 | 流水线 | 是 | sample 命名但仍是正式样本入口 | 保留 | 高 |
| `parse_cases_sample.py` | 解析样本案例原文 | 流水线 | 是 | 与 raw 数据绑定 | 保留 | 高 |
| `enrich_cases_sample.py` | 案例字段增强、罪名/法条/事实等抽取 | 流水线 | 是 | 规则较多，是质量关键点 | 保留 | 高 |
| `chunk_cases_sample.py` | 案例字段 chunk 生成 | 流水线 | 是 | chunk 策略是检索质量核心 | 保留 | 高 |
| `chunk_law_sample.py` | 法条 chunk 生成 | 流水线 | 是 | sample 命名但核心 | 保留 | 高 |
| `extract_amounts_sample.py` | 金额与金额角色抽取 | 流水线/运行时数据 | 是 | Amount role 未完全结构化，是中高风险债务 | 保留 | 高 |
| `build_graph_sample.py` | 构建 graph nodes/edges | 流水线 | 是 | 法条归一化和历史法条边是核心 | 保留 | 高 |
| `build_retrieval_chunks_sample.py` | 合并 law/case chunks 为检索语料 | 流水线 | 是 | 与 chunk 脚本耦合 | 保留 | 高 |
| `embed_retrieval_chunks_sample.py` | 构建向量索引 | 流水线 | 是 | 本地索引路径与模型版本需严格记录 | 保留 | 高 |
| `validate_sample_outputs.py` | 数据质量校验 | 评测/流水线 | 是 | 质量门禁，应保留 | 保留 | 高 |
| `make_manifest_sample.py` | 生成 manifest | 流水线 | 是 | 对可追溯性重要 | 保留 | 中 |
| `make_sample_report.py` | 样本报告生成 | 文档/流水线 | 否 | 辅助报告 | 保留 | 低 |
| `run_sample_pipeline.py` | 样本流水线总入口 | 流水线 | 是 | 当前最重要的数据重建入口 | 保留 | 高 |

### `processing/` 检索、图与回答

| 文件 | 作用 | 使用状态 | 核心路径 | 重复/废弃/技术债 | 删除建议 | 删除风险 |
|---|---|---:|---:|---|---|---:|
| `search_sample.py` | 词法检索和基础打分 | 运行时/评测 | 是 | 是 hybrid 的基础层，不是废弃 | 保留 | 高 |
| `vector_search_sample.py` | 向量检索 | 运行时/评测 | 是 | 与 hybrid 中 vector 分支耦合 | 保留 | 高 |
| `graph_retriever_sample.py` | 图候选召回和路径输出 | 运行时/评测 | 是 | graph 召回已参与候选生成，但仍可增强 | 保留 | 高 |
| `hybrid_search_sample.py` | 合并 lexical/vector/graph 的混合检索 | 运行时/评测 | 是 | 当前 API 检索核心，命名仍为 sample | 保留 | 高 |
| `answer_sample.py` | 早期 retrieval-only 答案基线 | 评测/遗留 | 否 | 与 chat_service/orchestration 职责重叠 | 暂不删除，可归档 | 中 |
| `langchain_orchestration_sample.py` | 当前实验/正式前端链路编排 | 运行时/评测 | 是 | 文件位置和 sample 命名与实际角色不匹配 | 保留，后续迁移 | 高 |
| `grounding_validator_sample.py` | 回答引用和 grounding 检查 | 评测/运行辅助 | 中 | 与 Provider 合同相关 | 保留 | 中 |

### `processing/` Router、Query Understanding 与训练

| 文件 | 作用 | 使用状态 | 核心路径 | 重复/废弃/技术债 | 删除建议 | 删除风险 |
|---|---|---:|---:|---|---|---:|
| `query_router.py` | 规则/LoRA 路由与检索意图判断 | 运行时 | 是 | 与 Query Understanding 有职责重叠 | 保留，后续收敛 | 高 |
| `query_understanding.py` | 统一 Query Understanding，含问题改写/实体/路由 | 运行时 | 是 | 当前方向正确，但规则后处理仍多 | 保留 | 高 |
| `scope_guard.py` | 刑法范围和拒答边界 | 运行时 | 是 | 与 router answer_type 有交叉 | 保留，后续明确边界 | 高 |
| `query_understanding_probe.py` | LoRA 能力探针 | 训练/验证 | 否 | 用于训练后检查 | 保留 | 中 |
| `collect_router_questions.py` | 收集 router 训练问题 | 训练 | 否 | 旧 router 数据链路 | 可归档 | 低 |
| `label_router_questions_deepseek.py` | 用 Provider 标注 router 数据 | 训练 | 否 | 旧 router 数据链路 | 可归档 | 低 |
| `correct_router_labels.py` | 修正 router 标签 | 训练 | 否 | 旧 router 数据链路 | 可归档 | 低 |
| `prepare_router_lora_dataset.py` | 旧 router LoRA 数据准备 | 训练 | 否 | 与新 QU 数据准备重复 | 可归档 | 中 |
| `train_router_lora.py` | 通用 LoRA 训练脚本 | 训练 | 是 | 仍被 `train_query_understanding_lora.py` 包装调用 | 保留 | 高 |
| `eval_router_lora.py` | 旧 router LoRA 评测 | 训练/评测 | 否 | 新 QU 后重要性下降 | 可归档 | 中 |
| `prepare_query_understanding_dataset.py` | QU 数据准备 | 训练 | 是 | 新训练链路核心 | 保留 | 中 |
| `augment_query_understanding_dataset.py` | QU 数据扩充 | 训练 | 是 | 需要区分 GPT 生成与规则生成来源 | 保留 | 中 |
| `merge_gpt_authored_dataset.py` | 合并 GPT 生成数据 | 训练 | 是 | 对可追溯性重要 | 保留 | 中 |
| `repair_gpt_authored_dataset.py` | 修复 GPT 生成 JSONL | 训练 | 是 | 训练数据清洗工具 | 保留 | 中 |
| `build_query_understanding_final_dataset.py` | 构建最终 QU 训练集 | 训练 | 是 | 当前训练集生成入口 | 保留 | 中 |
| `train_query_understanding_lora.py` | QU LoRA 快速训练包装器 | 训练 | 是 | 依赖 `train_router_lora.py`，命名需说明 | 保留 | 中 |
| `router_lora_README.md` | 旧 router LoRA 文档 | 文档 | 否 | 与新 QU 方向可能混淆 | 可归档或标注旧版 | 低 |
| `query_understanding_lora_TRAINING.md` | QU LoRA 训练说明 | 文档 | 是 | 当前训练操作文档 | 保留 | 低 |

### `processing/` 评测与诊断

| 文件 | 作用 | 使用状态 | 核心路径 | 重复/废弃/技术债 | 删除建议 | 删除风险 |
|---|---|---:|---:|---|---|---:|
| `search_eval_sample.py` | 词法检索评测 | 评测 | 是 | 回归必要 | 保留 | 中 |
| `vector_search_eval_sample.py` | 向量检索评测 | 评测 | 是 | 回归必要 | 保留 | 中 |
| `hybrid_eval_sample.py` | 混合检索评测 | 评测 | 是 | 回归必要 | 保留 | 中 |
| `graph_retriever_eval_sample.py` | 图召回评测 | 评测 | 是 | 回归必要 | 保留 | 中 |
| `answer_eval_sample.py` | answer baseline 评测 | 评测 | 中 | 与当前 orchestration 不完全一致 | 暂不删除 | 中 |
| `adversarial_eval_sample.py` | 对抗评测 | 评测 | 中 | 对拒答和边界有价值 | 保留 | 中 |
| `amount_semantic_eval_sample.py` | 金额语义评测 | 评测 | 中 | 金额角色问题重要 | 保留 | 中 |
| `langchain_orchestration_eval_sample.py` | orchestration mock 评测 | 评测 | 是 | 当前链路回归必要 | 保留 | 中 |
| `chat_vs_orchestration_eval.py` | stable 与 orchestration 对比 | 评测 | 中 | 发现链路漂移有价值 | 保留 | 中 |
| `replay_orchestration_failures.py` | 失败样例复现 | 评测/排障 | 中 | 回归排障工具 | 保留 | 低 |
| `embedding_diagnose_sample.py` | embedding 诊断 | 诊断 | 否 | 辅助工具 | 保留 | 低 |
| `minimax_smoke.py` | Provider smoke test | 诊断 | 否 | Provider 排障有价值 | 保留 | 低 |
| `download_qwen_model.py` | 下载本地模型 | 工具 | 否 | 训练/部署辅助 | 保留 | 低 |

### `frontend/`

| 文件 | 作用 | 使用状态 | 核心路径 | 重复/废弃/技术债 | 删除建议 | 删除风险 |
|---|---|---:|---:|---|---|---:|
| `frontend/package.json` | 前端依赖与脚本 | 运行时 | 是 | 需与锁文件保持一致 | 保留 | 高 |
| `frontend/package-lock.json` | 锁定前端依赖 | 运行时 | 是 | 保证可复现安装 | 保留 | 高 |
| `frontend/vite.config.ts` | Vite 配置 | 运行时 | 是 | 简洁 | 保留 | 高 |
| `frontend/tsconfig*.json` | TypeScript 配置 | 运行时 | 是 | 标准配置 | 保留 | 中 |
| `frontend/index.html` | 前端 HTML 入口 | 运行时 | 是 | 标准配置 | 保留 | 高 |
| `frontend/src/main.tsx` | React 入口 | 运行时 | 是 | 标准配置 | 保留 | 高 |
| `frontend/src/App.tsx` | 主 UI、会话、问答、右侧依据面板 | 运行时 | 是 | 文件承担职责较多，后续可拆分但不急 | 保留 | 高 |
| `frontend/src/api.ts` | API 客户端 | 运行时 | 是 | 当前默认应调用 orchestration 链路 | 保留 | 高 |
| `frontend/src/styles.css` | 全局样式 | 运行时 | 是 | UI 样式集中，文件偏大但可接受 | 保留 | 高 |
| `frontend/src/assets/*` | 静态资源 | 运行时 | 否 | Vite 默认资源 | 保留 | 低 |
| `frontend/README.md` | 前端说明 | 文档 | 否 | 可能需要更新运行说明 | 保留 | 低 |

### `tests/` 与 `docs/`

| 文件 | 作用 | 使用状态 | 核心路径 | 重复/废弃/技术债 | 删除建议 | 删除风险 |
|---|---|---:|---:|---|---|---:|
| `tests/test_api_chat.py` | API chat/orchestration/conversation 测试 | 测试 | 是 | 覆盖关键接口，但仍偏 happy path | 保留 | 高 |
| `docs/schema.md` | 样本数据 schema 文档 | 文档 | 是 | 应作为 processing_data 合同 | 保留 | 中 |
| `docs/langchain_orchestration_plan.md` | orchestration 设计计划 | 文档 | 否 | 可能落后于真实代码 | 可归档或更新 | 低 |

## 重点模块审计

### Retrieval

当前存在四个主要脚本：

- `search_sample.py`：词法检索，基础候选和规则打分。
- `vector_search_sample.py`：向量检索。
- `graph_retriever_sample.py`：图召回和 graph paths。
- `hybrid_search_sample.py`：合并 lexical/vector/graph 的实际检索入口。

判断：这些不是简单重复，而是 layered retrieval。但是命名和职责外观会让人误以为是多套旧版本。`hybrid_search_sample.py` 已是运行时核心，却仍位于 `processing/` 且带 sample 后缀，这是架构不一致。

风险：

- 排序权重和规则集中在 sample 脚本，缺少正式 runtime 包装。
- lexical/vector/graph 的 score/explain 字段合同不够集中。
- graph 既是召回器又提供解释路径，候选生成和解释展示边界仍需文档化。

### Answer

当前存在三层回答逻辑：

- `processing/answer_sample.py`：早期 retrieval-only baseline，主要用于 answer/adversarial eval。
- `app/llm/chat_service.py`：stable `/api/chat` 使用。
- `processing/langchain_orchestration_sample.py`：`/api/chat/orchestration` 使用，也是当前前端主链路。

判断：职责有明显重叠。它们都涉及检索、引用、拒答、fallback、答案类型或 evidence summary，只是稳定性、Provider 参与程度和前端用途不同。

风险：

- 同一问题在 stable 与 orchestration 链路可能表现不同。
- 修复一个链路时容易遗漏另一个链路。
- `answer_sample` 继续作为评测 baseline 有价值，但应标注为 legacy baseline。

### Router / Query Understanding

当前相关模块：

- `query_router.py`：旧 router/规则/LoRA 判断。
- `query_understanding.py`：统一 Query Understanding，输出问题改写、实体、检索范围、意图。
- `scope_guard.py`：刑法范围、拒答边界。
- `train_router_lora.py` 与 `train_query_understanding_lora.py`：训练入口复用。

判断：项目方向已经从“是否检索/检索什么”的 router，转向“多轮问题改写 + 实体识别 + 检索范围 + answer policy”的统一 Query Understanding。旧 router 仍可作为兼容层，但不应长期作为概念中心。

风险：

- 规则后处理较多，说明 LoRA 能力边界仍未完全覆盖。
- 新增 direct/general 类问题时，如果训练集未覆盖，LoRA 与规则可能分歧。
- scope guard 与 answer refusal 的边界需要更清晰：谁决定拒答，谁决定只给直接回答。

### Graph

当前相关模块：

- `build_graph_sample.py`：构建 nodes/edges。
- `graph_retriever_sample.py`：基于图的候选召回。
- `graph_retriever_eval_sample.py`：图召回评测。
- graph 数据产物在 `processing_data/`。

判断：图谱已不只是“解释路径”，已经参与 hybrid 候选生成。但 graph schema、graph retriever API 和 frontend 展示之间仍存在工程耦合。

风险：

- graph edge 的 raw/normalized/law_version 对历史法条非常关键，一旦构建逻辑漂移会影响法律正确性。
- graph path 展示对普通用户仍偏技术化，产品层需要转义为“相关案例/适用条文/参考路径”。
- 部分 graph 数据结构可能只在 eval/report 中使用，未形成统一数据访问层。

### API

当前接口：

- `/api/health`
- `/api/auth/login`
- `/api/retrieval/search`
- `/api/chat`
- `/api/chat/orchestration`
- `/api/conversations/*`

判断：接口可用，但 stable chat 和 orchestration chat 并存导致语义不够直观。当前前端主链路倾向 orchestration，`/api/chat` 更像稳定基线或兼容接口。

风险：

- `/api/chat/orchestration` 文件名和语义偏实验，但前端已经以它作为主要体验。
- 错误处理、audit log、chat history 三者之间需要统一事件模型。
- schema 模型集中在 `app/schemas.py` 是优势，应继续维护为唯一接口合同。

## 技术债清单

### High Priority

1. 运行时依赖 `processing/` sample 脚本  
   `app/processing_bridge.py` 把 `processing/` 加入 import path，说明数据处理脚本已经被运行时服务直接使用。这短期有效，但不适合生产化维护。

2. Answer 链路重复  
   `answer_sample.py`、`chat_service.py`、`langchain_orchestration_sample.py` 同时存在回答、引用、拒答、fallback 逻辑。后续最容易出现“修了 A，B 又坏”的问题。

3. Provider 稳定性和错误表达  
   Provider 调用失败、JSON contract、引用补齐、拒答纠偏是用户可感知风险。系统已有措施，但仍是最高风险模块之一。

4. Context Resolver 不完整  
   多轮对话中的“第一个案例”“刚才那个案子”“有没有类似案件”依赖历史解析和问题改写。当前已经引入 Query Understanding，但上下文对象化仍不足。

5. Amount Role 未完全结构化  
   金额匹配已能发挥作用，但金额类型、总额/单次、盗窃金额/罚金/退赔/量刑金额的角色区分仍会影响案例相似度。

### Medium Priority

1. `*_sample.py` 命名与真实角色不一致  
   很多 sample 文件已经是当前演示的核心路径，容易误导维护者。

2. 规则逻辑偏多  
   生活化表述、罪名别名、直接法律常识、拒答边界等存在规则兜底。规则是必要的，但需要集中管理和测试。

3. Prompt 分散  
   Prompt/JSON contract 分布在 `prompting.py`、`langchain_orchestration_sample.py`、Query Understanding 相关代码中。

4. 训练脚本新旧混用  
   `train_query_understanding_lora.py` 包装调用 `train_router_lora.py`，工程上复用合理，但命名上容易混淆。

5. 文档与真实架构可能漂移  
   `docs/langchain_orchestration_plan.md` 是计划文档，未必完全反映当前实现。

### Low Priority

1. 文件命名整理  
   后续可将运行时模块迁出 `processing/`，保留 pipeline 脚本在 `processing/`。

2. 前端组件拆分  
   `App.tsx` 承担状态、会话、输入、依据面板等多个职责。当前可运行，但长期可拆。

3. 诊断脚本归档  
   smoke、replay、diagnose 脚本可集中到 `tools/` 或 `processing/diagnostics/`。

4. 文档索引  
   根目录文档较多，建议后续增加 `docs/README.md` 说明每份文档用途。

## 删除/归档建议

本轮不建议直接删除任何源文件。当前项目仍处于样本级原型，很多“看似遗留”的文件承担回归、训练、排障或历史对比价值。

建议后续分阶段处理：

| 类别 | 文件/模块 | 建议 | 理由 |
|---|---|---|---|
| 可标注 legacy | `answer_sample.py` | 暂不删除，改为明确 baseline | 仍被 answer/adversarial eval 使用 |
| 可归档 | 旧 router 数据脚本 | 迁入 `processing/legacy_router/` | 新方向是 Query Understanding |
| 可归档 | `docs/langchain_orchestration_plan.md` | 标注“历史计划”或更新 | 避免与真实实现冲突 |
| 不建议删除 | `search/vector/graph/hybrid` | 保留 | 是 layered retrieval 核心 |
| 不建议删除 | `langchain_orchestration_sample.py` | 保留但后续迁移 | 当前前端主链路依赖 |

## 当前质量判断

### 是否存在评测过拟合风险？

存在中等风险。原因不是明显“刷题”，而是项目多轮优化高度围绕样本数据、adversarial eval、hidden eval 和用户手测问题展开。对 150 个案例范围内的体验已经明显增强，但泛化到更多罪名、更多金额形态、更多生活化上下文时仍需要扩大验证。

### 是否可以作为简历项目展示？

可以。建议展示定位为：

> 基于样本案例和刑法条文构建的刑法领域 GraphRAG 原型，覆盖数据处理、图谱构建、混合检索、Query Understanding、Provider 编排、前端证据展示和自动评测。

不要包装为生产级法律咨询系统。

### 是否达到高质量样本级 GraphRAG 原型？

基本达到。其优势在于工程闭环完整、评测意识强、可追溯性和 citation 设计较好。短板在于架构仍有 sample 遗留、上下文解析未完全对象化、Provider 稳定性和全量扩展能力尚未验证。

## 最可能失效的模块排序

1. Provider 编排：网络、JSON、拒答、引用、fallback 都会影响最终用户体验。
2. 多轮 Context Resolver：代词、上文案例、排序引用、金额追问容易出错。
3. 金额语义检索：金额角色和案件相似度未完全结构化。
4. Query Understanding LoRA 泛化：训练数据覆盖不足时仍依赖规则兜底。
5. Graph law normalization：历史法条、司法解释和现行法条映射一旦错误会造成法律事实错误。

## 审计结论

项目当前不是“代码不可维护”的状态，而是“原型演进成功后需要产品化收敛”的状态。最重要的下一步不是继续加零散规则，而是把运行时核心从 `processing/*_sample.py` 中抽离成正式模块，并用统一 Query Understanding、统一 Retrieval Contract、统一 Answer Contract 管住链路。
